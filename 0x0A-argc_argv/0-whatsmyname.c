#include <stdio.h>
/**
  *main - prints it name
  *@argc: number of commands
  *@argv: array of strings contains commands
  *Retun: 0 success
  */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
